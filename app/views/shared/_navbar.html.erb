<nav class="bg-blue-400 text-blue-100 text-xl px-4 py-2 flex justify-between items-center">
  <a href="/">
    <i class="fab fa-trello mr-1"></i>Tello
  </a>

  <div>
    <% if user_signed_in? %>
      <span class="mr-2"><%= current_user.email %></span>
      <%= link_to "登出", destroy_user_session_path, method: 'delete', class: 'nav-btn' %>
    <% else %>
      <%= link_to "登入", new_user_session_path, class: 'nav-btn' %>
      <%= link_to "註冊", new_user_registration_path, class: 'nav-btn' %>
    <% end %>
  </div>
</nav>